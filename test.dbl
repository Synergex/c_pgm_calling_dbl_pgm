; This is the mainline for the program.
; The C routine sub1 is called with three arguments.
; The first argument is an alpha passed by descriptor.
; The second argument is decimal passed by descriptor.
; The third argument is numeric passed by value.
; The returned value is displayed.
.main
external function
	sub1,	^val
record
	val,	i4
.proc
	open(1, o, "TT:")
	val = %sub1("text", 3, ^val(4))
	writes(1, "main: val = " + %string(val))
	close 1
	stop
.end
